import { PrimaryCasePipe } from './primary-case.pipe';

describe('PrimaryCasePipe', () => {
  it('create an instance', () => {
    const pipe = new PrimaryCasePipe();
    expect(pipe).toBeTruthy();
  });
});
